"use client";

export default function LoginForm() {
  return (
    <section>
      <p>WebAuthn login and registration controls go here.</p>
      <button type="button">Register with passkey</button>
      <button type="button">Sign in with passkey</button>
    </section>
  );
}