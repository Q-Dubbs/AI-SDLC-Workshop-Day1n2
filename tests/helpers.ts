import type { Page } from "@playwright/test";

export async function loginWithPasskey(_: Page): Promise<void> {
  // TODO: add shared virtual authenticator login flow.
}

export async function createTodo(_: Page, __: string): Promise<void> {
  // TODO: add reusable todo creation helper.
}